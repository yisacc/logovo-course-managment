import en from 'src/shared/message/languages/en';
import id from 'src/shared/message/languages/id';

export default {
    en,
    id,
};

export enum ENUM_MESSAGE_LANGUAGE {
    en = 'en',
    id = 'id',
}
